type User {
    email: String
    firstName: String
    lastName: String
}

input UserInput {
    email: String!
    firstName: String!
    lastName: String!
}

type Query {
    users: [User]
}

type Mutation {
    addUsers(input: [UserInput]): Boolean
}

schema {
    query: Query
    mutation: Mutation
}

